inputs:
  gateway_url:
    description: 'The URL of the Ignition Gateway (e.g. http://localhost:8088)'
    required: true
    default: 'http://localhost:8088'
  api_key:
    description: 'The API key for the Ignition Gateway'
    required: true

runs:
  using: node20
  main: dist/index.js